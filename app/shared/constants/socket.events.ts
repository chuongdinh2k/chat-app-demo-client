export enum CollectionSocketEvents {}

export enum ChatEvents {
  sendMessage = "sendMessage",
  receiveMessage = "receiveMessage",
  isTyping = "isTyping",
  getIsTyping = "getIsTyping",
}
export enum RoomEvents {
  LEAVE_ROOM = "leave-room",
  JOIN_ROOM = "join-room",
}
